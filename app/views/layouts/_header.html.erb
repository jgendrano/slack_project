<nav class="bar-no-margin" data-topbar role="navigation">

    <p class="name">
      <%= link_to image_tag("slacktracklogo.png"), landings_path %>
    </p>
  <ul class="title-area">
    <li class="toggle-topbar">
      <% if user_signed_in? %>
        <% if !(current_user.name == nil) %>
          <li> Signed in as <b><%= current_user.name %></b> on Team <b><%= @team_name %></b></li>
          <li><b><%= link_to 'Channels', channels_path %> ||
          <%= link_to 'Pinned Messages', pins_path %></li></b>
        <% else %>
          <li> Signed in on Team <b><%= @team_name %></b> </li>
          <li><b><%= link_to 'Channels', channels_path %> ||
          <%= link_to 'Pinned Messages', pins_path %></li></b>
        <% end %>
        <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %> <b>||</b> <%= link_to "Sign Into Another Team", '/users/auth/slack' %></li>
      <% else %>
        <li><%= link_to 'Sign In', '/users/auth/slack' %></li>

      <% end %>
    </li>

  </ul>
</nav>
