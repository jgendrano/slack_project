
<div class="row">

  <div class="large-12 columns test-box test-move" style="background-color:white;">
    <center><h2 class="channel-title">#<%= @channel.channel_name%></h2><b><h2 class="roboto-stuff"> - Recent Messages</h2>
    </center></b><br>
    <% if !current_user.nil? %>
      <ul>
      <% @messages.each do |message| %>

        <li><b><%= "#{message.slack_username}"%></b> <%=
        "(#{DateTime.strptime(message.ts, '%s').strftime('%m/%d/%Y at %I:%M%p')}):
         #{message.slack_message}"%>
        <!-- <%= link_to "Save", pins_path(message), method: :post %> -->
        <%= link_to "Save", pins_path(data: { text: message.slack_message,
          user: message.slack_username, ts: message.ts }), method: :post %>
       </li>
      <% end %>
      <b><center><center><b><h5><%= link_to "Return to Slack Channels", channels_path %></h5><br>
    </ul>
    <% end %>
  </div>

</div>
</body>
